name: CI

on:
    push:
        branches:
            - master
            - main
env:
    WF_ENV: ${{ env.ENVIRONMENT }}

jobs:
    sample-job: 
        runs-on: ubuntu-latest
        env:
            JOB_ENV: ${{ secrets.API_KEY }}

        steps:
        - name: get env
          env:
            STEP_ENV: Available to this step
          run: |
            echo "WF_ENV: $WF_ENV"
            echo "JOB_ENV: ${JOB_ENV}"
            echo "STEP_ENV: ${STEP_ENV}"


